################################################################################
# GitLab CI for the AI Figma module on git.drupalcode.org.
#
# Uses the Drupal GitLabCI template (the DrupalCI replacement), which provides
# composer, PHPCS, PHPStan and PHPUnit jobs across the supported and adjacent
# core versions with no per-project maintenance. See:
#   https://project.pages.drupalcode.org/gitlab_templates/
################################################################################

include:
  - project: $_GITLAB_TEMPLATES_REPO
    ref: $_GITLAB_TEMPLATES_REF
    file:
      - '/includes/include.drupalci.main.yml'
      - '/includes/include.drupalci.variables.yml'
      - '/includes/include.drupalci.workflows.yml'

variables:
  # Test against adjacent core versions too, not just the current one.
  OPT_IN_TEST_PREVIOUS_MINOR: 1
  OPT_IN_TEST_NEXT_MINOR: 1
  OPT_IN_TEST_NEXT_MAJOR: 1
  OPT_IN_TEST_MAX_PHP: 1
  # No CSS in this module; the webship-js (Playwright + Cucumber) functional
  # suite under tests/ runs in the dedicated webship-js job below.
  SKIP_STYLELINT: 1
  SKIP_ESLINT: 1

# Blocking quality gates: Drupal + DrupalPractice coding standards and PHPStan.
phpcs:
  allow_failure: false

phpstan:
  allow_failure: false

# Spelling is advisory - a dictionary miss should not block the pipeline.
cspell:
  allow_failure: false

# webship-js functional lanes. Follows the proven webshare CI setup: build a
# site with this module, serve it with Apache against a real MySQL service, and
# run the tests/ webship-js (Playwright + Cucumber) BDD suite. Two jobs run the
# SAME suite in the two ecosystems the module targets - Drupal Canvas AI (Drupal
# CMS) and Display Builder AI (standard profile + display_builder_base recipe).
# Advisory (allow_failure) while stabilising; the suite is green locally 22/22.
.webship-js:
  extends: .testing-job-base
  stage: test
  needs: []
  allow_failure: false
  services:
    - name: $_CONFIG_DOCKERHUB_ROOT/$_TARGET_DB_TYPE-$_TARGET_DB_VERSION:$_TARGET_DB_IMAGE_TAG
      alias: database
  variables:
    LAUNCH_URL: "http://localhost"
    LOGIN_USER: "webmaster"
    LOGIN_PASS: "dD.123123ddd"
    HEADLESS: "true"
    BROWSER: "chromium"
    FORCE_COLOR: "1"
    _BUILD_DIR: "/tmp/site-build"
    SITE_PACKAGE: "drupal/cms"
    INSTALL_PROFILE: "drupal_cms_installer"
    EXTRA_REQUIRE: ""
    COMPANION_RECIPE: ""
    ENABLE_MODULES: "ai_figma"
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success
    - if: '$CI_COMMIT_REF_NAME =~ /^[0-9]+\.[0-9]+\.x$/'
      when: on_success
    - when: never
  artifacts:
    when: on_failure
    paths:
      - tests/videos
      - tests/screenshots
      - tests/reports
    expire_in: 1 week
  before_script:
    # Node 22 + Playwright (yarn 4 needs node-modules linker; webship-js's
    # postinstall can clobber cucumber.js, so back it up around yarn install).
    - curl -fsSL https://deb.nodesource.com/setup_22.x | bash -
    - apt-get install -y nodejs
    - 'test -f .yarnrc.yml || echo "nodeLinker: node-modules" > .yarnrc.yml'
    - cp cucumber.js .cucumber.js.bak
    - corepack enable
    - yarn install
    - cp .cucumber.js.bak cucumber.js && rm .cucumber.js.bak
    - ./node_modules/.bin/playwright install --with-deps chromium
    # Build a fresh site with this module via a Composer path repository.
    - composer create-project "$SITE_PACKAGE" $_BUILD_DIR --no-interaction --no-progress
    - cd $_BUILD_DIR
    - composer config minimum-stability dev
    - composer config prefer-stable true
    - composer config allow-plugins true -n
    - composer config repositories.mut '{"type":"path","url":"'"$CI_PROJECT_DIR"'","options":{"symlink":true}}'
    - composer require "drupal/ai_figma:*@dev" drush/drush $EXTRA_REQUIRE --no-interaction --no-progress
    # Apache serves the built site against the MySQL service.
    - mkdir -pv $_BUILD_DIR/web/sites/default/files $_BUILD_DIR/web/sites/simpletest
    - chmod -R 777 $_BUILD_DIR/web/sites/default/files $_BUILD_DIR/web/sites/simpletest
    - rm -rf /var/www/html && ln -s $_BUILD_DIR/web /var/www/html
    - chown -R www-data:www-data $_BUILD_DIR
    - service apache2 start
    - export SIMPLETEST_DB=mysql://$MYSQL_USER:$MYSQL_PASSWORD@database/$MYSQL_DATABASE
    # Install, normalise the admin to "webmaster", apply any companion recipe,
    # then enable the module and the ecosystem's modules.
    - $_BUILD_DIR/vendor/bin/drush --root=$_BUILD_DIR/web site:install $INSTALL_PROFILE --db-url=$SIMPLETEST_DB --account-name=webmaster --account-pass=dD.123123ddd --site-name="AI Figma CI" -y
    - $_BUILD_DIR/vendor/bin/drush --root=$_BUILD_DIR/web php:eval '$u=\Drupal\user\Entity\User::load(1); $u->setUsername("webmaster"); $u->setEmail("webmaster@example.test"); $u->setPassword("dD.123123ddd"); $u->save();'
    - if [ -n "$COMPANION_RECIPE" ]; then RD=$(find $_BUILD_DIR -type f -name recipe.yml -path "*/$COMPANION_RECIPE/*" | head -1); [ -n "$RD" ] && $_BUILD_DIR/vendor/bin/drush --root=$_BUILD_DIR/web recipe "$(dirname "$RD")" -y || echo "recipe $COMPANION_RECIPE not found"; fi
    - $_BUILD_DIR/vendor/bin/drush --root=$_BUILD_DIR/web en $ENABLE_MODULES -y
    - $_BUILD_DIR/vendor/bin/drush --root=$_BUILD_DIR/web cache:rebuild
  script:
    - cd "$CI_PROJECT_DIR"
    - yarn test

# Drupal Canvas AI ecosystem: Drupal CMS + the Canvas AI stack.
'webship-js (Drupal Canvas AI)':
  extends: .webship-js
  variables:
    ENABLE_MODULES: "ai_figma canvas_ai ai_agents ai_assistant_api"

# Display Builder AI ecosystem: standard profile + the display_builder_base
# recipe, the way the Display Builder AI project's own functional tests set up.
'webship-js (Display Builder AI)':
  extends: .webship-js
  variables:
    SITE_PACKAGE: "drupal/recommended-project"
    INSTALL_PROFILE: "standard"
    EXTRA_REQUIRE: "drupal/display_builder_ai"
    COMPANION_RECIPE: "display_builder_base"
    ENABLE_MODULES: "ai_figma display_builder_ai"


# Build the docs/ folder with MkDocs and publish it to GitLab Pages, served at
# https://project.pages.drupalcode.org/ai_figma/. On merge requests the job just
# builds (validating the docs with --strict); GitLab only publishes Pages from
# the default branch. `--strict` fails the job on a broken link or nav entry.
pages:
  stage: build
  needs: []
  image: python:3.12-slim
  rules:
    - if: '$CI_COMMIT_TAG'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success
    - if: '$CI_COMMIT_REF_NAME =~ /^[0-9]+\.[0-9]+\.x$/'
      when: on_success
    - when: never
  script:
    - pip install --no-cache-dir mkdocs-material
    - mkdocs build --strict --site-dir public
  artifacts:
    paths:
      - public
